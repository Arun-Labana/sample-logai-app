<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Console appender for local viewing -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- LogAI Remote Appender - only appId is required! -->
    <appender name="LOGAI" class="com.logai.remote.RemoteLogAppender">
        <!-- Get your App ID from: https://logai-frontend-app.vercel.app/settings -->
        <appId>0f445744-70ba-4422-9d97-7ecde2ac8eb3</appId>
        
        <!-- Optional: Only send WARN and above (default is WARN) -->
        <threshold>WARN</threshold>
    </appender>

    <!-- Root logger -->
    <root level="DEBUG">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="LOGAI" />
    </root>

</configuration>
